
services:
  postgres:
    image: postgres:13-alpine

    environment:
      POSTGRES_DB: card_db
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql
